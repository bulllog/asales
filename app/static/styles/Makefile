#This is make file for styles

#src path

base = base.css
components = components.css
filter = filter.css
home = home.css
items = items.css
item = item.css
payment = payment.css
popup = popup.css
slider = slider.css
table = table.css
carousel = carousel.css

files = ${base} ${components} ${home} ${payment} ${items} ${item} ${table} ${popup} ${slider} ${filter} ${carousel}

asales.css: ${files}
	cat > $@ $^

