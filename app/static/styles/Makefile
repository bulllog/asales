#This is make file for styles

#src path

base = base.css
components = components.css
home = home.css
payment = payment.css
items = items.css
table = table.css
popup = popup.css

files = ${base} ${components} ${home} ${payment.css} ${components} ${items} ${table} ${popup}


asales.css: ${files}
	cat > $@ $^


