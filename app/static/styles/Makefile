#This is make file for styles

#src path

base = base.css
components = components.css
home = home.css
payment = payment.css

files = ${base} ${components} ${home} ${payment.css}

asales.css: ${files}
	cat > $@ $^


